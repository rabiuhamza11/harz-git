# TRUE-MODE KILLER TEST — PRE-REGISTERED ACCEPTANCE PLAN (TT-1..TT-10)
**Frozen Sep 15, 2026 BEFORE execution (research rule #8: pre-register thresholds before experiments).**
**Precondition: the REAL ceremony has run (successor + 3 witnesses generated, papers in independent
custody, manifest signed on Node 1, verify-chain ACCEPTS). D8 ruled: succession freezes first.**

The software mode (KT-1..KT-10, 10/10) proved the CODE survives node death. True mode proves the
EVENT: real keys, real papers, real Node 1, live root. Thresholds are binary PASS/REFUSED — no
partial credit, no post-hoc reinterpretation.

TT-1 CEREMONY ARTIFACTS REAL: successor/witness PUBs committed to HarzGit under
harz-root-v2/succession/manifest/; manifest signature over canonical bytes VERIFIED by the kit;
NO private key bytes anywhere in git (leak sweep clean). PASS = all committed + verified + clean.

TT-2 PAPER CUSTODY INDEPENDENT (witness-attested): the 3 witness paper sets are in 3 different
custodies (two buildings minimum, per D6). PASS = owner attests custody on record; a single hand
holding all three = NO-GO (Y3 quorum-theater law).

TT-3 PAPER RECOVERY WORKS: combine any 2 of 3 successor papers → byte-exact key → signs a test
message → verifies against the committed successor PUB. PASS = sign+verify clean. (Do this during
the ceremony, BEFORE the manifest signing — Y2 ordering.)

TT-4 NODE 1 DEATH: with Node 1's ZSK declared dead (ceremony records the compromise height),
holders verify: pre-death signatures still VALID (honest window), post-death self-declared keys
REFUSED, successor zone only enthroned WITH 2-of-3 witness quorum over the death act.

TT-5 ZOMBIE REFUSAL (true mode): the old ZSK attempts a post-revocation zone. PASS = REFUSED
through every door (kit verify-chain + resolver with the succession chain).

TT-6 OFFLINE SURVIVAL: airplane-mode holder with the last good zone in cache still resolves all
77 names, NXDOMAIN stays honest. PASS = 77/77 + honest nulls, zero network.

TT-7 QR-RAIL TRANSFER OF THE CHAIN: the zone CHAIN since the last anchor-signed zone crosses via
camera (z_old + z_successor). Holder runs verify-chain → ACCEPTS. A single successor zone alone
REFUSED (availability law, honest by design). PASS = chain accepted, single-zone refused.

TT-8 LIVE MIGRATION GATE: after the killer test passes, the v2 zone (production signature on
Node 1 per migration order) deploys: 77/77 parity with v1 book, doors re-verified (DoH, /go/,
extension), fail-closed law intact (NXDOMAIN 404, tamper refused).

TT-9 MIGRATION REVERSIBILITY: before deploying, the v1 zone + worker source are pinned in
HarzGit (rollback artifact). PASS = pin exists + byte-verified. (Fork-refusal discipline: a
rollback is a NEW signed zone, never a silent history rewrite.)

TT-10 HONEST LABELS: report states exactly what true mode proved: the named events, on the named
devices, with the named custody — nothing more. "The namespace survived X" only if X actually died.

RUN ORDER: TT-1..TT-3 at ceremony; TT-4..TT-7 the death test; TT-8..TT-9 migration day; TT-10 always.
